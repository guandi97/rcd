rcd() { 
	if [ -z "$2" ]; then
		lst=$(ls -R | grep ./ | egrep "^\.(/[^/]+){$1}$")
		grep -n "" <<< "$lst"

		read in
		cd="`pwd`${$(awk -v nr=$in 'NR==nr' <<< "$lst"):1:-1}"
	        cd $cd
                
	 	return
	fi
          
	for i in {1..$1..1}; do
		cd $2
	done    
}
